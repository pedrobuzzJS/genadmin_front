import api from "./api";

const apiHelper = class {
  private options: any;

  constructor(options: any) {
    this.options = options;
  }

  get() {
    return new Promise( ( resolve, reject ) => {

    } );
  }
};